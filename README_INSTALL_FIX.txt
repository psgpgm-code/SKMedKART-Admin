Install App Prompt Fix
- Removed the unwanted instruction alert popup.
- Install button now appears only when Chrome provides the real PWA install prompt.
- Clicking Install App triggers Chrome's native install prompt directly.
- Button hides after installation or when the prompt is unavailable.
- Existing billing, backup/restore and other app features were not changed.
